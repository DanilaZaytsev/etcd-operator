# Patterns to ignore when building packages.
*.tmpl
.git
.gitignore
*.tgz
README.md.gotmpl
# helm-unittest suites. They exercise the chart, they are not part of what it
# installs, and shipping them bloats the published package.
tests/
